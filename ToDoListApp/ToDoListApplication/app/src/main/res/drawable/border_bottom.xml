<!-- res/drawable/border_bottom.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Background color (optional) -->
    <stroke
        android:color="@android:color/black"
    android:width="2dp"/>                   <!-- Border thickness -->
    <size
        android:height="1dp" />  <!-- Optional: defines the height of the bottom border -->
</shape>
